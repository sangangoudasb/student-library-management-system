package com.example.student.library.management.system.enums;

public enum CardStatus {              // enum means predefined data type where it will be constant.
                                         //for ex: while filling application there will be dropdown  male and female
                                         // in that option only you need to select only one option.

    ACTIVE,
    INACTIVE,
    BLOCKED

}
